<?xml version="1.0" encoding="UTF-8"?>

<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.5/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <!-- Configuration d'exécution -->
        <ini name="display_errors" value="1"/>
        <ini name="error_reporting" value="-1"/>

        <!-- Variables d'environnement -->
        <server name="KERNEL_CLASS" value="App\Kernel"/>
        <server name="APP_ENV" value="test" force="true"/>
        <server name="APP_SECRET" value="testsecret123"/>
        <server name="SHELL_VERBOSITY" value="-1"/>
        <server name="DATABASE_URL_TEST" value="mysql://root:@127.0.0.1:3306/talenteko_test?serverVersion=mariadb-10.4.32&amp;charset=utf8mb4"/>

        <!-- Mailer en mémoire -->
        <server name="MAILER_DSN" value="array://"/>
        <server name="APP_MAILER_FROM" value="no-reply@talenteko.test"/>

        <!-- Messenger synchrone -->
        <server name="MESSENGER_TRANSPORT_DSN" value="sync://"/>
    </php>

    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="functional">
            <directory>tests/Functional</directory>
        </testsuite>
    </testsuites>

    <coverage>
        <report>
            <html outputDirectory="coverage" lowUpperBound="50" highLowerBound="90"/>
            <text outputFile="php://stdout" showUncoveredFiles="false"/>
        </report>
    </coverage>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true">
        <include>
            <directory suffix=".php">src</directory>
        </include>
    </source>
</phpunit>
