APP_ENV=dev
APP_DEBUG=1
APP_SECRET=ChangeMeForLocalDev

# Connexion DB locale (adapter user/password selon ton environnement)
DATABASE_URL="mysql://user:password@127.0.0.1:3306/talenteko?charset=utf8mb4"

# Base séparée pour les tests (optionnelle en local)
DATABASE_URL_TEST="mysql://user:password@127.0.0.1:3306/talenteko_test?charset=utf8mb4"

# Mailer local (exemple avec Mailhog, Mailtrap ou Mailpit)
# Exemple Mailhog : smtp://127.0.0.1:1025
# Exemple Mailtrap : smtp://user:password@smtp.mailtrap.io:2525
MAILER_DSN="smtp://user:password@127.0.0.1:1025"

# Messenger (transport Doctrine par défaut en local)
MESSENGER_TRANSPORT_DSN=doctrine://default

# Expéditeur par défaut pour les emails
APP_MAILER_FROM=no-reply@talenteko.test

# URL de base de l'application en local
APP_BASE_URL="http://127.0.0.1:8000"
