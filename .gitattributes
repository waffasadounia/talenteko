# ================================================
# .gitattributes Ã¢â‚¬â€ TalentÃƒâ€°kÃƒÂ´
# Objectif :
# - Uniformiser les fins de ligne (LF) dans le dÃƒÂ©pÃƒÂ´t
# - Ãƒâ€°viter les diffs parasites Windows/Linux/Mac
# - ProtÃƒÂ©ger les binaires
# - Forcer UTF-8 sans BOM pour le code
# ================================================

# Normaliser toutes les fins de ligne en LF dans le dÃƒÂ©pÃƒÂ´t
* text=auto eol=lf

# Scripts Windows (gardent CRLF cÃƒÂ´tÃƒÂ© working copy si besoin)
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# Scripts Unix (toujours LF)
*.sh   text eol=lf

# Fichiers texte/documentation (toujours LF)
*.md   text eol=lf
.editorconfig text eol=lf

# Fichiers dÃ¢â‚¬â„¢environnement (important : pas de CRLF)
.env*  text eol=lf

# Binaires (pas de normalisation Ã¢â€ â€™ stockÃƒÂ©s tels quels)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.woff binary
*.woff2 binary
*.ttf  binary
*.eot  binary
*.mp4  binary
*.mov  binary

# SVG : texto (diff lisible), on garde LF
*.svg  text eol=lf

# Lockfiles Ã¢â€ â€™ forcer LF (ÃƒÂ©viter conflits Windows)
composer.lock     text eol=lf
package-lock.json text eol=lf
symfony.lock      text eol=lf
.editorconfig text eol=lf
prettier.config.js text eol=lf


# Fichiers code (UTF-8 sans BOM + LF)
*.php   text working-tree-encoding=UTF-8
*.twig  text working-tree-encoding=UTF-8
*.yaml  text working-tree-encoding=UTF-8
*.yml   text working-tree-encoding=UTF-8
*.json  text working-tree-encoding=UTF-8
