# ================================================
# .gitattributes — TalentÉkô
# Objectifs :
# - Uniformiser les fins de ligne (LF) dans le dépôt
# - Éviter les diffs parasites Windows/Linux/Mac
# - Protéger les binaires
# ================================================

# Normaliser toutes les fins de ligne en LF dans le dépôt
* text=auto eol=lf

# Scripts Windows (gardent CRLF côté working copy si besoin)
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# Scripts Unix (toujours LF)
*.sh   text eol=lf

# Fichiers texte/documentation (toujours LF)
*.md            text eol=lf
.editorconfig   text eol=lf
.gitattributes  text eol=lf
.gitignore      text eol=lf
*.patch         text eol=lf

# Fichiers d'environnement (important : pas de CRLF)
.env*  text eol=lf

# Lockfiles – forcer LF (éviter conflits Windows)
composer.lock      text eol=lf
package-lock.json  text eol=lf
symfony.lock       text eol=lf

# Fichiers code (toujours LF)
*.php   text eol=lf
*.twig  text eol=lf
*.yaml  text eol=lf
*.yml   text eol=lf
*.json  text eol=lf
*.css   text eol=lf
*.scss  text eol=lf

# SVG : texto (diff lisible), on garde LF
*.svg  text eol=lf

# Binaires (pas de normalisation – stockés tels quels)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.woff binary
*.woff2 binary
*.ttf  binary
*.eot  binary
*.mp4  binary
*.mov  binary
