# config/packages/doctrine_migrations.yaml
doctrine_migrations:
    migrations_paths:
        # Namespace rÃ©servÃ© aux migrations Doctrine (ne pas utiliser App\â€¦)
        'DoctrineMigrations': '%kernel.project_dir%/migrations'

    # AtomicitÃ© : une migration Ã©choue â†’ rollback complet
    all_or_nothing: true

    # Classement automatique des migrations dans des sous-dossiers (annÃ©e/mois)
    organize_migrations: BY_YEAR_AND_MONTH

    # DÃ©sactivÃ© en prod pour Ã©viter les surcharges
    enable_profiler: false
