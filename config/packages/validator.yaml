# config/packages/validator.yaml
framework:
    validation:
        email_validation_mode: html5

        # Active lÃ¢â‚¬â„¢auto-mapping pour toutes les entitÃƒÂ©s App\Entity
        auto_mapping:
            App\Entity\: []

when@test:
    framework:
        validation:
            # En test : pas de vÃƒÂ©rification contre haveibeenpwned
            not_compromised_password: false
