# config/packages/validator.yaml
framework:
    validation:
        email_validation_mode: html5

        # Active lâ€™auto-mapping pour toutes les entitÃ©s App\Entity
        auto_mapping:
            App\Entity\: []

when@test:
    framework:
        validation:
            # En test : pas de vÃ©rification contre haveibeenpwned
            not_compromised_password: false
