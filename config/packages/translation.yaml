# config/packages/translation.yaml
framework:
  # Langue par dÃ©faut de l'application
  default_locale: fr

  translator:
    # Dossier oÃ¹ se trouvent les fichiers de traduction (.yaml, .xlf, .php)
    default_path: '%kernel.project_dir%/translations'

    # Si une traduction est manquante â†’ repli automatique sur le franÃ§ais
    fallbacks:
      - fr
