# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
  secret: '%env(APP_SECRET)%'

  # dÃƒÆ’Ã‚Â©sactive totalement les annotations (routes, etc.)
  annotations: false

  # EmpÃƒÆ’Ã‚Âªche les overrides HTTP type _method
  http_method_override: false

  # Intercepte toutes les erreurs PHP (y compris fatales)
  handle_all_throwables: true

  # Protection CSRF activÃƒÆ’Ã‚Â©e globalement (sÃƒÆ’Ã‚Â©curitÃƒÆ’Ã‚Â© des formulaires)
  csrf_protection: true

  # Sessions
  session:
    handler_id: null
    cookie_secure: auto
    cookie_samesite: lax

  # Journalisation des erreurs PHP
  php_errors:
    log: true

# Configuration spÃƒÆ’Ã‚Â©cifique ÃƒÆ’Ã‚Â  lÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢environnement de test
when@test:
  framework:
    test: true
    session:
      storage_factory_id: session.storage.factory.mock_file


