# config/services.yaml
parameters:
  # On force Symfony ÃƒÆ’Ã‚Â  interprÃƒÆ’Ã‚Â©ter APP_MAILER_FROM comme une string
  app.mailer_from: '%env(string:APP_MAILER_FROM)%'

services:
  _defaults:
    autowire: true
    autoconfigure: true

  App\:
    resource: '../src/'
    exclude:
      - '../src/DependencyInjection/'
      - '../src/Entity/'
      - '../src/Kernel.php'
      - '../src/Tests/'

  App\Validator\ValidLocationValidator:
    arguments:
      $appEnv: '%kernel.environment%'


