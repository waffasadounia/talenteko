#!/bin/sh

echo "🔍 Vérification du code avec lint-staged..."
npx lint-staged || exit 1

echo "🧩 Vérification de composer.json..."
composer validate --no-check-publish || exit 1

echo "🧪 Lancement des tests unitaires..."
php bin/phpunit --testsuite=unit --stop-on-failure || exit 1

echo "✅ Tous les checks sont passés, commit validé !"

