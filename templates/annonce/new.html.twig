{% extends 'base.html.twig' %}

{% block title %}Déposer une annonce | TalentÉkô{% endblock %}

{% block body %}
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6">{{ page_title }}</h1>

    <p class="mb-6">
      Ici viendra le formulaire de création d’annonce (MVP : AnnonceType, validation, CSRF, etc.).
    </p>

    <a href="{{ path('app_home') }}"
       class="border border-orangePeche text-orangePeche
              px-4 py-2 rounded-full font-semibold transition
              hover:bg-orange-100 hover:text-orangePeche
              focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-orangePeche"
       aria-label="Revenir à l’accueil">
      ← Retour à l’accueil
    </a>
  </main>
{% endblock %}
