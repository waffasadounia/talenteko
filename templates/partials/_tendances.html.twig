<section id="tendances" class="bg-white py-16">
  <div class="mx-auto max-w-6xl px-4">
    <h2 class="text-talenteko-bleue-900 mb-10 text-center text-3xl font-bold">
      Tendances actuelles
    </h2>

    <div
      class="grid grid-cols-1 gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4"
    >
      {% for listing in listings %}
        {% include 'partials/_listing_card.html.twig' with {
          listing: listing
        } %}
      {% else %}
        <p class="col-span-full text-center text-gray-500 italic">
          Aucune annonce disponible pour l'instant.<br />
          <a href="{{ path('app_listing_new') }}" class="btn-link">
            <i class="fa fa-plus-circle"></i>
            <span>Dépose le premier !</span>
          </a>
        </p>
      {% endfor %}
    </div>

    <div class="mt-8 text-center">
      <a
        href="{{ path('app_search') }}"
        class="btn-link"
        aria-label="Voir tous les listings"
      >
        <i class="fa fa-search"></i>
        <span>Voir toutes les annonces</span>
      </a>
    </div>
  </div>
</section>
