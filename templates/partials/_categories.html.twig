{#================================
  partials/_categories.html.twig
  Section catÃƒÂ©gories populaires
  ================================#}

<section id="categories" class="py-16 bg-orange-50">
	<div class="max-w-6xl mx-auto px-4">
		<h2 class="text-3xl font-bold mb-10 text-center text-shadow-talenteko-blue-900">
			CatÃƒÂ©gories populaires
		</h2>
		<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
			{% for category in categories %}
				{% include 'partials/_category_card.html.twig' with {category: category} %}
			{% else %}
				<p class="text-center col-span-full text-gray-500 italic">
					Aucune catÃƒÂ©gorie disponible pour lÃ¢â‚¬â„¢instant.
				</p>
			{% endfor %}
		</div>

		<div class="mt-8 text-center">
			<a href="{{ path('app_category_index') }}" class="btn-link" aria-label="Voir toutes les catÃƒÂ©gories">
				<i class="fa fa-list-ul"></i>
				<span>Toutes les catÃƒÂ©gories</span>
			</a>
		</div>
	</div>
</section>

