{# ======================================
   Partiel : Messages flash
   TalentÉkô — version modernisée
   ====================================== #}

{% for label, messages in app.flashes %}
    {% for message in messages %}
        {% include 'partials/_alert.html.twig' with {
            type: label,
            message: message
        } only %}
    {% endfor %}
{% endfor %}
