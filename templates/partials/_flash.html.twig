{# ================================
  partials/_flash.html.twig
  Messages flash (succès, erreurs, etc.)
  ================================ #}

{% for label, messages in app.flashes %}
  {% for message in messages %}
    {% include 'partials/_alert.html.twig' with {
      type: label,    # ex: success, error, warning, info
      message: message
    } only %}
  {% endfor %}
{% endfor %}
