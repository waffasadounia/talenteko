{#================================
  templates/category/show.html.twig
  Page dÃ©taillÃ©e d'une catÃ©gorie
  ================================#}

{% extends 'base.html.twig' %}

{% block title %}
  {{ category.name }}
  | TalentÃ©ko
{% endblock %}

{% block body %}
  <section class="bg-white py-16">
    <div class="mx-auto max-w-6xl px-4">
      <h1 class="text-talenteko-blue mb-10 text-center text-3xl font-bold">
        {{ category.name }}
      </h1>

      {# Grille des listings liÃ©s Ã  cette catÃ©gorie #}
      <div
        class="grid grid-cols-1 gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4"
      >
        {% for listing in category.listings %}
          {% include 'partials/_listing_card.html.twig' with {
            listing: listing
          } %}
        {% else %}
          <p class="col-span-full text-center text-gray-500 italic">
            Aucun listing pour cette catÃ©gorie pour l'instant.
          </p>
        {% endfor %}
      </div>

      <div class="mt-8 text-center">
        <a href="{{ path('app_category_index') }}" class="btn-link">
          <i class="fa fa-arrow-left"></i>
          <span>Retour aux catÃ©gories</span>
        </a>
      </div>
    </div>
  </section>
{% endblock %}
