{% extends 'base.html.twig' %}

{% block title %}
  Admin | Tableau de bord TalentÉkô
{% endblock %}

{% block body %}
  <section class="mx-auto max-w-6xl px-2">
    <h1 class="mb-2 text-3xl font-bold text-talenteko-blue-900 pb-4">
      Tableau de bord administrateur
    </h1>
{% include 'admin/_admin_nav.html.twig' %}
    <div class="mt-10 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm transition hover:shadow-md hover:-translate-y-1">
        <p class="text-sm text-gray-500">Utilisateurs</p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">{{ totalUsers ?? '—' }}</p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm transition hover:shadow-md hover:-translate-y-1">
        <p class="text-sm text-gray-500">Annonces actives</p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">{{ totalListings ?? '—' }}</p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm transition hover:shadow-md hover:-translate-y-1">
        <p class="text-sm text-gray-500">Échanges en cours</p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">{{ totalExchanges ?? '—' }}</p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm transition hover:shadow-md hover:-translate-y-1">
        <p class="text-sm text-gray-500">Messages récents</p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">{{ totalMessages ?? '—' }}</p>
      </div>
    </div>

    <p class="mt-10 text-gray-700">
      Bienvenue dans l’espace d’administration de <strong>TalentÉkô</strong>. Utilisez le menu
      ci-dessus pour accéder aux différentes sections.
    </p>
  </section>
{% endblock %}
