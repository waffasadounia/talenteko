{% extends 'base.html.twig' %}
{% block title %}
  Admin | Tableau de bord TalentÉkô
{% endblock %}

{% block body %}
  <section class="mx-auto max-w-6xl px-4 py-10">
    <h1 class="mb-8 text-3xl font-bold text-talenteko-blue-900">Tableau de bord administrateur</h1>

    <nav aria-label="Navigation principale admin" class="flex flex-wrap gap-4">
      <a href="{{ path('app_admin_user_index') }}" class="btn-outline">Utilisateurs</a>
      <a href="{{ path('app_admin_listing_index') }}" class="btn-outline">Annonces</a>
      <a href="{{ path('app_admin_exchange_index') }}" class="btn-outline">Échanges</a>
      <a href="{{ path('app_admin_message_index') }}" class="btn-outline">Messages</a>
      <a href="{{ path('app_styleguide_buttons') }}" class="btn-link">Styleguide UI</a>
    </nav>

    <div class="mt-10 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm">
        <p class="text-sm text-gray-500">
          Utilisateurs
        </p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">
          {{ totalUsers ?? '—' }}
        </p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm">
        <p class="text-sm text-gray-500">
          Annonces actives
        </p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">
          {{ totalListings ?? '—' }}
        </p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm">
        <p class="text-sm text-gray-500">
          Échanges en cours
        </p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">
          {{ totalExchanges ?? '—' }}
        </p>
      </div>

      <div class="rounded-xl border border-gray-200 bg-white p-6 text-center shadow-sm">
        <p class="text-sm text-gray-500">
          Messages récents
        </p>
        <p class="text-2xl font-semibold text-talenteko-blue-900">
          {{ totalMessages ?? '—' }}
        </p>
      </div>
    </div>

    <p class="mt-10 text-gray-700">
      Bienvenue dans l’espace d’administration de <strong>TalentÉkô</strong>. Utilisez le menu
      ci-dessus pour accéder aux différentes sections.
    </p>
  </section>
{% endblock %}
