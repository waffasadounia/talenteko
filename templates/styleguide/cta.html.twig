{# templates/styleguide/cta.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Styleguide — CTA | Talentékô{% endblock %}

{% block body %}
  {% include 'styleguide/_nav.html.twig' %}

  <main class="container mx-auto px-6 py-12 space-y-12">
    <header class="space-y-2">
      <h1 class="text-3xl font-bold text-primary-color">Styleguide — CTA</h1>
      <p class="text-gray-600">
        Variantes réutilisables : primary, outline, link, tailles, états.
        Focus et hover accessibles respectés.
      </p>
    </header>

    {# Variantes #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Variantes</h2>
      <div class="flex flex-wrap items-center gap-4">
        <a href="#" class="btn-primary" aria-label="CTA principal">
          <i class="fa fa-user" aria-hidden="true"></i><span>Primary</span>
        </a>
        <a href="#" class="btn-outline" aria-label="CTA secondaire">
          <i class="fa fa-plus" aria-hidden="true"></i><span>Outline</span>
        </a>
        <a href="#" class="btn-link" aria-label="CTA texte">Lien (tertiaire)</a>
      </div>
      <p class="text-sm text-gray-500">
        Primary = CTA fort · Outline = CTA secondaire · Link = action de texte.
      </p>
    </section>

    {# Tailles #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Tailles</h2>
      <div class="flex flex-wrap items-center gap-4">
        <a class="btn-primary btn-sm" href="#" aria-label="CTA petit">Primary .btn-sm</a>
        <a class="btn-primary" href="#" aria-label="CTA normal">Primary</a>
        <a class="btn-primary btn-lg" href="#" aria-label="CTA grand">Primary .btn-lg</a>
      </div>
      <div class="flex flex-wrap items-center gap-4">
        <a class="btn-outline btn-sm" href="#" aria-label="CTA secondaire petit">Outline .btn-sm</a>
        <a class="btn-outline" href="#" aria-label="CTA secondaire normal">Outline</a>
        <a class="btn-outline btn-lg" href="#" aria-label="CTA secondaire grand">Outline .btn-lg</a>
      </div>
    </section>

    {# Icônes seules + block #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Icônes & pleine largeur</h2>
      <div class="flex flex-wrap items-center gap-4">
        <button class="btn-icon bg-talenteko-orange-500 text-white" aria-label="Ouvrir le menu">
          <i class="fa fa-bars" aria-hidden="true"></i>
        </button>
        <button class="btn-icon border border-talenteko-orange-500 text-talenteko-orange-500" aria-label="Ajouter aux favoris">
          <i class="fa fa-heart" aria-hidden="true"></i>
        </button>
      </div>
      <div class="max-w-md">
        <a class="btn-primary btn-block" href="#" aria-label="CTA pleine largeur">
          <i class="fa fa-rocket" aria-hidden="true"></i><span>Lancer l’action</span>
        </a>
      </div>
      <p class="text-sm text-gray-500">
        `.btn-icon` pour les boutons à icône seule · `.btn-block` pour pleine largeur (mobile/form).
      </p>
    </section>

    {# États #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">États</h2>
      <div class="flex flex-wrap items-center gap-4">
        <button class="btn-primary is-disabled" disabled aria-disabled="true">Primary (désactivé)</button>
        <button class="btn-outline is-disabled" disabled aria-disabled="true">Outline (désactivé)</button>
        <button class="btn-primary btn-loading" type="button" aria-busy="true">
          <i class="fa fa-spinner fa-spin" aria-hidden="true"></i>
          <span>Chargement…</span>
        </button>
      </div>
      <p class="text-sm text-gray-500">
        `.is-disabled` désactive les interactions · `.btn-loading` ajoute un spinner et bloque les clics.
      </p>
    </section>

    {# Exemples concrets #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Exemples d’usage</h2>
      <div class="flex flex-wrap items-center gap-4">
        <a href="{{ path('app_listing_new') }}" class="btn-primary" aria-label="Déposer une annonce">+ Déposer une annonce</a>
        <a href="#tendances" class="btn-outline" aria-label="Découvrir les talents">Découvrir les talents</a>
        <a href="{{ path('app_search') }}" class="btn-link" aria-label="Voir toutes les annonces">Voir toutes les annonces →</a>
      </div>
    </section>

    {# Accessibilité #}
    <section class="space-y-3">
      <h2 class="text-xl font-semibold">Accessibilité</h2>
      <ul class="list-disc pl-6 text-gray-800 space-y-1">
        <li>Un seul <strong>CTA fort</strong> par zone.</li>
        <li>Focus visible sur tous les CTA (inclus dans les classes utilitaires).</li>
        <li>Icônes décoratives avec <code>aria-hidden="true"</code> ; si action → accompagnées d’un texte clair.</li>
        <li>Animations discrètes ; utilisables avec <code>motion-safe:</code> pour respecter la préférence utilisateur.</li>
      </ul>
    </section>
  </main>
{% endblock %}
