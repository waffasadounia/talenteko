{# templates/styleguide/cta.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Styleguide Ã¢â‚¬â€ CTA | TalentÃƒÂ©kÃƒÂ´
{% endblock %}

{% block body %}
	{% include 'styleguide/_nav.html.twig' %}

	<main class="container mx-auto px-6 py-12 space-y-12">
		<header class="space-y-2">
			<h1 class="text-3xl font-bold text-primary-color">Styleguide Ã¢â‚¬â€ CTA</h1>
			<p class="text-gray-600">
				Variantes rÃƒÂ©utilisables : primary, outline, link, tailles, ÃƒÂ©tats.
								Focus et hover accessibles respectÃƒÂ©s.
			</p>
		</header>

		{# Variantes #}
		<section class="space-y-4">
			<h2 class="text-xl font-semibold">Variantes</h2>
			<div class="flex flex-wrap items-center gap-4">
				<a href="#" class="btn-primary" aria-label="CTA principal">
					<i class="fa fa-user" aria-hidden="true"></i>
					<span>Primary</span>
				</a>
				<a href="#" class="btn-outline" aria-label="CTA secondaire">
					<i class="fa fa-plus" aria-hidden="true"></i>
					<span>Outline</span>
				</a>
				<a href="#" class="btn-link" aria-label="CTA texte">Lien (tertiaire)</a>
			</div>
			<p class="text-sm text-gray-500">
				Primary = CTA fort Ã‚Â· Outline = CTA secondaire Ã‚Â· Link = action de texte.
			</p>
		</section>

		{# Tailles #}
		<section class="space-y-4">
			<h2 class="text-xl font-semibold">Tailles</h2>
			<div class="flex flex-wrap items-center gap-4">
				<a class="btn-primary btn-sm" href="#" aria-label="CTA petit">Primary .btn-sm</a>
				<a class="btn-primary" href="#" aria-label="CTA normal">Primary</a>
				<a class="btn-primary btn-lg" href="#" aria-label="CTA grand">Primary .btn-lg</a>
			</div>
			<div class="flex flex-wrap items-center gap-4">
				<a class="btn-outline btn-sm" href="#" aria-label="CTA secondaire petit">Outline .btn-sm</a>
				<a class="btn-outline" href="#" aria-label="CTA secondaire normal">Outline</a>
				<a class="btn-outline btn-lg" href="#" aria-label="CTA secondaire grand">Outline .btn-lg</a>
			</div>
		</section>

		{# IcÃƒÂ´nes seules + block #}
		<section class="space-y-4">
			<h2 class="text-xl font-semibold">IcÃƒÂ´nes & pleine largeur</h2>
			<div class="flex flex-wrap items-center gap-4">
				<button class="btn-icon bg-talenteko-orange-500 text-white" aria-label="Ouvrir le menu">
					<i class="fa fa-bars" aria-hidden="true"></i>
				</button>
				<button class="btn-icon border border-talenteko-orange-500 text-talenteko-orange-500" aria-label="Ajouter aux favoris">
					<i class="fa fa-heart" aria-hidden="true"></i>
				</button>
			</div>
			<div class="max-w-md">
				<a class="btn-primary btn-block" href="#" aria-label="CTA pleine largeur">
					<i class="fa fa-rocket" aria-hidden="true"></i>
					<span>Lancer l'action</span>
				</a>
			</div>
			<p class="text-sm text-gray-500">
				`.btn-icon` pour les boutons ÃƒÂ  icÃƒÂ´ne seule Ã‚Â· `.btn-block` pour pleine largeur (mobile/form).
			</p>
		</section>

		{# Ãƒâ€°tats #}
		<section class="space-y-4">
			<h2 class="text-xl font-semibold">Ãƒâ€°tats</h2>
			<div class="flex flex-wrap items-center gap-4">
				<button class="btn-primary is-disabled" disabled aria-disabled="true">Primary (dÃƒÂ©sactivÃƒÂ©)</button>
				<button class="btn-outline is-disabled" disabled aria-disabled="true">Outline (dÃƒÂ©sactivÃƒÂ©)</button>
				<button class="btn-primary btn-loading" type="button" aria-busy="true">
					<i class="fa fa-spinner fa-spin" aria-hidden="true"></i>
					<span>ChargementÃ¢â‚¬Â¦</span>
				</button>
			</div>
			<p class="text-sm text-gray-500">
				`.is-disabled` dÃƒÂ©sactive les interactions Ã‚Â· `.btn-loading` ajoute un spinner et bloque les clics.
			</p>
		</section>

		{# Exemples concrets #}
		<section class="space-y-4">
			<h2 class="text-xl font-semibold">Exemples d'usage</h2>
			<div class="flex flex-wrap items-center gap-4">
				<a href="{{ path('app_listing_new') }}" class="btn-primary" aria-label="DÃƒÂ©poser une annonce">+ DÃƒÂ©poser une annonce</a>
				<a href="#tendances" class="btn-outline" aria-label="DÃƒÂ©couvrir les talents">DÃƒÂ©couvrir les talents</a>
				<a href="{{ path('app_search') }}" class="btn-link" aria-label="Voir toutes les annonces">Voir toutes les annonces Ã¢â€ â€™</a>
			</div>
		</section>

		{# AccessibilitÃƒÂ© #}
		<section class="space-y-3">
			<h2 class="text-xl font-semibold">AccessibilitÃƒÂ©</h2>
			<ul class="list-disc pl-6 text-gray-800 space-y-1">
				<li>Un seul
					<strong>CTA fort</strong>
					par zone.</li>
				<li>Focus visible sur tous les CTA (inclus dans les classes utilitaires).</li>
				<li>IcÃƒÂ´nes dÃƒÂ©coratives avec
					<code>aria-hidden="true"</code>
					; si action accompagnÃƒÂ©es d'un texte clair.</li>
				<li>Animations discrÃƒÂ¨tes ; utilisables avec
					<code>motion-safe:</code>
					pour respecter la prÃƒÂ©fÃƒÂ©rence utilisateur.</li>
			</ul>
		</section>
	</main>
{% endblock %}
