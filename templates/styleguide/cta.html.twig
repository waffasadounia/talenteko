{# templates/styleguide/cta.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}
  Styleguide CTA | TalentÃƒÂ©ko
{% endblock %}

{% block body %}
  {% include 'styleguide/_nav.html.twig' %}

  <main class="container mx-auto space-y-12 px-6 py-12">
    <header class="space-y-2">
      <h1 class="text-primary-color text-3xl font-bold">Styleguide CTA | TalentÃƒÂ©ko</h1>
      <p class="text-gray-600">
        Variantes rÃƒÂ©utilisables : primary, outline, link, tailles, ÃƒÂ©tats. Focus
        et hover accessibles respectent les normes d'accessibilitÃƒÂ©.
      </p>
    </header>

    {# Variantes #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Variantes</h2>
      <div class="flex flex-wrap items-center gap-4">
        <a href="#" class="btn-primary" aria-label="CTA principal">
          <i class="fa fa-user" aria-hidden="true"></i>
          <span>Primary</span>
        </a>
        <a href="#" class="btn-outline" aria-label="CTA secondaire">
          <i class="fa fa-plus" aria-hidden="true"></i>
          <span>Outline</span>
        </a>
        <a href="#" class="btn-link" aria-label="CTA texte">Lien (tertiaire)</a>
      </div>
      <p class="text-sm text-gray-500">
        Primary = CTA fort (sans lift) <code>talenteko-orange-500</code> (avec lift) <code>talenteko-orange-400</code>
        (avec lift) <code>talenteko-orange-400</code> (sans lift) <code>talenteko-orange-500</code>
      </p>
    </section>

    {# Tailles #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Tailles</h2>
      <div class="flex flex-wrap items-center gap-4">
        <a class="btn-primary btn-sm" href="#" aria-label="CTA petit">
          Primary .btn-sm
        </a>
        <a class="btn-primary" href="#" aria-label="CTA normal">Primary</a>
        <a class="btn-primary btn-lg" href="#" aria-label="CTA grand">
          Primary .btn-lg
        </a>
      </div>
      <div class="flex flex-wrap items-center gap-4">
        <a
          class="btn-outline btn-sm"
          href="#"
          aria-label="CTA secondaire petit"
        >
          Outline .btn-sm
        </a>
        <a class="btn-outline" href="#" aria-label="CTA secondaire normal">
          Outline
        </a>
        <a
          class="btn-outline btn-lg"
          href="#"
          aria-label="CTA secondaire grand"
        >
          Outline .btn-lg
        </a>
      </div>
    </section>

    {# IcÃƒÂ´nes seules + block #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">IcÃƒÂ´nes seules + block</h2>
      <div class="flex flex-wrap items-center gap-4">
        <button
          class="btn-icon bg-talenteko-orange-500 text-white"
          aria-label="Ouvrir le menu"
        >
          <i class="fa fa-bars" aria-hidden="true"></i>
        </button>
        <button
          class="btn-icon border-talenteko-orange-500 text-talenteko-orange-500 border"
          aria-label="Ajouter aux favoris"
        >
          <i class="fa fa-heart" aria-hidden="true"></i>
        </button>
      </div>
      <div class="max-w-md">
        <a
          class="btn-primary btn-block"
          href="#"
          aria-label="CTA pleine largeur"
        >
          <i class="fa fa-rocket" aria-hidden="true"></i>
          <span>Lancer l'action</span>
        </a>
      </div>
      <p class="text-sm text-gray-500">
        `.btn-icon` pour les boutons d'icÃƒÂ´nes seules + block
      </p>
    </section>

    {# IcÃƒÂ´nes seules + block #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">IcÃƒÂ´nes seules + block</h2>
      <div class="flex flex-wrap items-center gap-4">
        <button class="btn-primary is-disabled" disabled aria-disabled="true">
          Primary (disabled)
        </button>
        <button class="btn-outline is-disabled" disabled aria-disabled="true">
          Outline (disabled)
        </button>
        <button class="btn-primary btn-loading" type="button" aria-busy="true">
          <i class="fa fa-spinner fa-spin" aria-hidden="true"></i>
          <span>Chargement...</span>
        </button>
      </div>
      <p class="text-sm text-gray-500">
        `.is-disabled` dÃƒÂ©sactive les interactions
      </p>
    </section>

          <i class="fa fa-spinner fa-spin" aria-hidden="true"></i>
          <span>Chargement...</span>
        </button>
      </div>
      <p class="text-sm text-gray-500">
        `.is-disabled` dÃƒÂ©sactive les interactions
      </p>
    </section>

    {# Exemples concrets #}
    <section class="space-y-4">
      <h2 class="text-xl font-semibold">Exemples d'usage</h2>
      <div class="flex flex-wrap items-center gap-4">
        <a
          href="{{ path('app_listing_new') }}"
          class="btn-primary"
          aria-label="DÃƒÂ©pÃƒÂ´t d'annonce"
        >
          + DÃƒÂ©poser une annonce
        </a>
        <a
          href="#tendances"
          class="btn-outline"
          aria-label="DÃƒÂ©couvrir les talents"
        >
          + DÃƒÂ©couvrir les talents
        </a>
        >
          DÃƒÂ©couvrir les talents
        </a>
        <a
          href="{{ path('app_search') }}"
          class="btn-link"
          aria-label="Voir toutes les annonces"
        >
          Voir toutes les annonces
        </a>
      </div>
    </section>

    {# AccessibilitÃƒÂ© #}
    <section class="space-y-3">
      <h2 class="text-xl font-semibold">AccessibilitÃƒÂ©</h2>
      <ul class="list-disc space-y-1 pl-6 text-gray-800">
        <li>
          Un seul
          <strong>CTA fort</strong>
          par zone.
        </li>
        <li>
          Focus visible sur tous les CTA (inclus dans les classes utilitaires).
        </li>
        <li>
          IcÃƒÂ´nes dÃƒÂ©coratives avec
          <code>aria-hidden="true"</code>
          ; si action accompagnÃƒÂ©e d'un texte clair.
        </li>
        <li>
          Animations discrÃƒÂ¨tes ; utilisables avec
          <code>motion-safe:</code>
          pour respecter la prÃƒÂ©fÃƒÂ©rence utilisateur.
        </li>
      </ul>
    </section>
  </main>
{% endblock %}
