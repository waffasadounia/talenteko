{% extends 'base.html.twig' %}

{% block title %}Styleguide — Boutons | TalentÉkô{% endblock %}

{% block body %}
{% include 'styleguide/_nav.html.twig' %}

  <main class="container mx-auto px-6 py-12 space-y-10">
    <header class="space-y-2">
      <h1 class="text-3xl font-bold text-primary">Styleguide — Boutons</h1>
      <p class="text-gray-600">Page interne pour tester les composants UI TalentÉkô (boutons & liens).</p>
    </header>

    <section class="space-y-3">
      <h2 class="text-xl font-semibold">Bouton principal (plein)</h2>
      <div class="flex items-center gap-4">
        <a href="#" class="btn-primary" aria-label="Exemple bouton plein">
          <i class="fa fa-user" aria-hidden="true"></i>
          <span>Se connecter</span>
        </a>
        <a href="#" class="btn-primary" aria-label="Exemple bouton plein long">
          <i class="fa fa-check" aria-hidden="true"></i>
          <span>Valider mon inscription</span>
        </a>
      </div>
      <p class="text-sm text-gray-500">Normal: bg <code>talenteko-orange-500</code> · Hover: <code>talenteko-orange-400</code> + lift · Focus ring visible.</p>
    </section>

    <section class="space-y-3">
      <h2 class="text-xl font-semibold">Bouton secondaire (outline)</h2>
      <div class="flex items-center gap-4">
        <a href="#" class="btn-outline" aria-label="Exemple bouton outline">+ Déposer une annonce</a>
        <a href="#" class="btn-outline" aria-label="Exemple bouton outline alt">
          <i class="fa fa-plus" aria-hidden="true"></i>
          <span>Publier</span>
        </a>
      </div>
      <p class="text-sm text-gray-500">Normal: bordure/texte <code>talenteko-orange-500</code> · Hover: fond clair + texte orange.</p>
    </section>

    <section class="space-y-3">
      <h2 class="text-xl font-semibold">Bouton lien (tertiaire)</h2>
      <div class="flex items-center gap-6">
        <a href="#" class="btn-link" aria-label="Lien retour">← Retour à l’accueil</a>
        <a href="#" class="btn-link" aria-label="Lien voir plus">
          <span>Voir plus</span>
        </a>
      </div>
      <p class="text-sm text-gray-500">Texte orange 500 · Hover: orange 600 + souligné · Focus ring visible.</p>
    </section>

    <section class="space-y-3">
      <h2 class="text-xl font-semibold">Navigation principale (liens)</h2>
      <nav aria-label="Navigation principale" class="flex items-center gap-6">
        <a href="#" class="btn-link"><i class="fa fa-search" aria-hidden="true"></i><span>Recherche</span></a>
        <a href="#" class="btn-link"><i class="fa fa-heart" aria-hidden="true"></i><span>Favoris</span></a>
        <a href="#" class="btn-link"><i class="fa fa-envelope" aria-hidden="true"></i><span>Messages</span></a>
      </nav>
    </section>

    <section class="space-y-3">
      <h2 class="text-xl font-semibold">État focus & accessibilité</h2>
      <p class="text-gray-600">Teste avec <kbd>Tab</kbd> : chaque bouton a un focus ring bien visible. Les icônes ont <code>aria-hidden="true"</code> et les liens/CTA ont des <code>aria-label</code> pertinents.</p>
    </section>
  </main>
{% endblock %}
